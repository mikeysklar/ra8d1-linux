# Overlay for measuring SDRAM with the Cortex-M85 data cache off, so we can
# quantify what the cache is buying us:
#   west build -b ek_ra8d1 <app> -- -DEXTRA_CONF_FILE=nocache.conf
CONFIG_DCACHE=n
CONFIG_CACHE_MANAGEMENT=n
